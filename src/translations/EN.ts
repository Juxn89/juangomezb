import { Info } from "@/common";
import { Translation } from "./Translations.interface";

export const EN: Translation = Object.freeze({
	Summary: {
		greeting: `Hi!, I'm Juan`,
		description:
			`Fullstack developer, focused on .NET technologies with ${ Info.yearsOfExperience }+ years of experience in the industry.`
	}
})